syntax = "proto3";

package greeter;

// The service contract (compiled into Rust traits/clients by tonic-build).
service Greeter {
  rpc SayHello (HelloRequest) returns (HelloReply);
}

message HelloRequest {
  string name = 1;
}

message HelloReply {
  string message = 1;
}
